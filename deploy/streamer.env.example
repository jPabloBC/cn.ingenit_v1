# Example environment for streamer service
# Copy to /etc/streamer/streamer.env and fill values before starting the service

PORT=4000
NODE_ENV=production

# Tokens: either set a static token for quick testing, or set a signing key for HMAC-signed ephemeral tokens
STREAMER_TOKEN=
STREAMER_SIGNING_KEY=

# Optional: Supabase admin (only if you use Supabase for auth in APIs)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Playwright options
PLAYWRIGHT_HEADFUL=false
PLAYWRIGHT_BROWSER=chromium
# Example environment for streamer service
# Copy to /etc/streamer/streamer.env and edit values

PORT=4000
NODE_ENV=production

# Static token for development (optional)
STREAMER_TOKEN=

# HMAC signing key for ephemeral tokens (recommended in production)
STREAMER_SIGNING_KEY=

# Optional: supabase settings if used by APIs (leave empty if not)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
